# План добавления `logformat: raw | json` в danted

Дата: 2026-09-13.

Статус: этапы 1–6 реализованы. JSON-логирование проверено на macOS arm64
и Linux arm64 (Debian 12); полная сверка production-кода с ТЗ завершена.
Обычные записи, события соединений, сигнальные сообщения, stack trace и
дампы отладочного буфера используют JSON после его активации. Ранние записи
до чтения директивы остаются raw согласно контракту.

[Итоговый отчёт](../features/logformat-json/acceptance.md) содержит матрицу
соответствия, результаты проверок, overhead и демонстрацию реальным curl.
[Руководство](../features/logformat-json/README.md) описывает включение формата.

## 1. Цель и результат

Добавить в серверный конфигурационный файл глобальную директиву
`logformat`, которая управляет форматом всех записей собственного логгера
Dante. По умолчанию сохраняется существующий текстовый формат `raw`.
При `logformat: json` журналы становятся JSON Lines, а события соединений
получают отдельные поля для аналитики без разбора текста `message`.

```conf
logformat: json
logoutput: /var/log/danted.log
errorlog: /var/log/danted-error.log
```

Выбранный подход: общий JSON-сериализатор и передача структурных данных
из мест формирования событий. Простая JSON-обёртка вокруг существующих
сообщений недостаточна: адреса, правила, причины завершения, длительность
и счётчики должны быть доступны отдельно.

Ориентировочный объём: **8–12 рабочих дней для одного разработчика**,
включая тестирование Linux/macOS. Это предварительная инженерная оценка,
а не результат реализации или измерений производительности.

## 2. Основания и карта изменений

План подготовлен по статическому анализу структуры исходников, вызовов
журналирования по проекту, конфигурационного парсера, распространения
конфигурации между процессами и существующего CI. При подготовке плана
сборка и runtime-тесты не запускались.

Все пути ниже указаны относительно каталога `dante/` в корне репозитория.

| Подсистема | Основные файлы | Значение для реализации |
|---|---|---|
| Общий логгер | `lib/log.c`, `lib/serr.c` | `slog()` / `vslog()` и `signalslog()` сходятся в `dolog()`; здесь находятся получатели, raw-префиксы, stack trace и отладочный буфер |
| Конфигурация | `lib/config_scan.l`, `lib/config_parse.y`, `include/sockd.h` | Директива, допустимые значения и серверное поле формата |
| Жизненный цикл конфигурации | `sockd/serverconfig.c`, `sockd/mother_util.c`, `sockd/sockd_child.c`, `sockd/shmemconfig.c` | Сброс настроек, SIGHUP, shared memory, копирование в дочерние процессы и `compareconfigs()` |
| Диагностика конфигурации | `lib/showconfig.c`, `lib/parse_util.c` | Отображение активного формата и ошибки разбора |
| События соединений | `lib/log.c`, `sockd/sockd_request.c`, `sockd/sockd_negotiate.c`, `sockd/sockd_tcp.c`, `sockd/dante_udp.c` | `iolog()` получает правило, состояние соединения и адресные структуры |
| Закрытие и снимки соединений | `sockd/sockd_io.c` | `io_delete()` и `siginfo()` формируют часть журналов напрямую через `slog()`, обходя `iolog()` |
| Сборка | `lib/Makefile.am`, `sockd/Makefile.am` и соответствующие генерируемые файлы | Общие исходники собираются для сервера и клиента; новые серверные интерфейсы не должны нарушить клиентскую сборку |
| Тестирование и CI | `sockd/tests/`, `ci/smoke.py`, `ci/build-package.sh`, `.github/workflows/packages.yml` в корне репозитория | Модульные проверки, сетевые сценарии, сборка пакетов и smoke tests |
| Пользовательская документация | `doc/sockd.conf.5`, `example/sockd.conf`, `example/sockd-basic.conf` | Описание директивы и примеры включения |

Существующий JSON-код statistics API не является готовой заменой логгеру:
для журналов необходимы экранирование произвольных строк, обработка длинных
записей и отдельные ограничения сигнального пути.

## 3. Зафиксированные решения и границы

### Конфигурация

- Директива доступна только в глобальной части серверного конфига.
- Допустимые значения: `raw` и `json`; значение по умолчанию — `raw`.
- Неизвестное значение и повторная директива являются ошибками конфигурации.
- Формат един для `logoutput` и `errorlog`, включая файлы, stdout/stderr
  и содержимое сообщения, передаваемого в syslog.
- Выбор событий через правила, уровни важности и маршрутизация по получателям
  сохраняются. Включение JSON само по себе не включает дополнительные события.
- Raw сохраняет существующие сообщения и их оформление.
- Клиентские библиотеки и клиентский конфиг не получают новую настройку.
- CLI-вывод справки и версии остаётся существующим; это не записи логгера.

### Первый запуск и SIGHUP

- При первом запуске до чтения `logformat` действует raw. Директиву
  рекомендуется размещать первой, чтобы последующая диагностика была JSON.
- Предварительное отдельное чтение формата до основного парсера не добавляется.
- При SIGHUP до чтения новой директивы действует прежний формат; прочитанное
  корректное значение переключает формат в процессе, выполняющем разбор.
- Если директива отсутствует, после успешного разбора устанавливается raw.
- Дочерние процессы получают значение существующим механизмом обновления
  конфигурации через shared memory.
- Обновление процессов асинхронное: при переходе допустимо временное смешение
  raw и JSON в одном получателе.
- Существующая обработка ошибок перечитывания сохраняется. Транзакционный
  rollback всей конфигурации не входит в работу.

### Вне этой версии

- Сквозной идентификатор сессии между процессами.
- Структурирование каждого произвольного диагностического сообщения.
- Преобразование платформозависимого текста TCP_INFO в числовую схему.
- Новый формат statistics API или новые метрики этого API.
- Новая runtime-зависимость на JSON-библиотеку.
- Изменение внешнего оформления и политики хранения syslog-сервиса.

## 4. Контракт JSON

### 4.1. Общие поля и транспорт

В файлах и stdout/stderr одна запись — один JSON-объект с одним завершающим
переводом строки. Вложенные переводы строк экранируются. Текстовый raw-префикс
перед объектом отсутствует.

В syslog передаётся JSON-объект без текстового префикса Dante. Внешний syslog
заголовок добавляется системным сервисом; его файл целиком не обязан быть
JSON Lines. Уровень и facility продолжают передаваться существующим способом.

| Поле | Тип | Смысл |
|---|---|---|
| `schema_version` | integer | Версия схемы, первоначально `1` |
| `timestamp` | string | Unix time в виде `seconds.microseconds`, ровно шесть цифр после точки |
| `level` | string | Текстовый уровень важности |
| `pid` | integer | PID процесса, создавшего запись |
| `process` | string | Роль процесса |
| `program` | string | Имя программы |
| `event` | string | Тип события; `message` для общей диагностики |
| `message` | string | Человекочитаемое сообщение без стандартного raw-префикса |
| `truncated` | boolean | Признак усечения при формировании JSON-записи |

Иллюстрация общей диагностической записи; пример записан одной строкой:

```json
{"schema_version":1,"timestamp":"1789257600.123456","level":"info","pid":1234,"process":"mother","program":"danted","event":"message","message":"server ready","truncated":false}
```

`truncated` отражает усечение, обнаруженное при построении события и JSON.
Он не может обнаруживать изменение записи внешним syslog-сервисом.

### 4.2. Структурные события соединений

Для операций `iolog()` выделяются типы событий:

- `accept`, `hostid`, `connect`;
- `block`, `temporary_block`;
- `disconnect`, `error`, `temporary_error`;
- `io`.

Доступные поля передаются из исходных C-структур, без разбора готовых
строк журналов регулярными выражениями:

- `rule.number`, `rule.type`, `verdict`, `protocol`, `command`;
- `source`, `destination`, `source_proxy`, `destination_proxy`;
- известный метод аутентификации и идентификатор пользователя;
- дополнительные данные конкретного события и известная ошибка.

Каждый адресный объект содержит доступные `local` и `peer`. Endpoint включает
тип адреса, адрес без порта и числовой порт. Представление должно поддерживать
IPv4, IPv6 и доменное имя, сохраняя различие локального и удалённого адреса
и адресов промежуточного proxy.

Блокировка правилом отличается от ошибки соединения. Текст `message`
сохраняется для человека, но не служит источником структурных полей.

Неизвестные значения опускаются; известное нулевое значение сохраняется.
Пароли и содержимое credential-структур не сериализуются. Payload включается
только при существующем разрешении `log: data`. TCP_INFO остаётся текстовым
полем с корректным экранированием.

Реализованные имена полей этапа 3:

| Поле | Содержимое и условия |
|---|---|
| `rule` | `number` (включая 0), `type`: `client-rule`, `hostid-rule`, `socks-rule` или `monitor`, если тип известен |
| `verdict` | `block` для block/temporary_block; иначе исходный verdict правила. Ошибка I/O не меняет разрешающее правило на блокирующее |
| `protocol`, `command` | Исходные значения; псевдокоманды Dante сохраняются, неизвестная команда опускается |
| `source`, `destination`, `source_proxy`, `destination_proxy` | Известные `local`/`peer` с `type` (`ipv4`/`ipv6`/`domain`), `address`, `port`; для IPv6 также `scope_id`, включая 0 |
| `authentication` внутри адресного объекта | `method` и доступный непустой `user`; credential-объекты и пароли не передаются сериализатору |
| `hostids` внутри адресного объекта | Известные адреса промежуточных узлов, если поддержка hostid включена в сборке |
| `detail` | Дополнительный текст не-I/O события |
| `error.code` | Исходный errno для вызовов, в которых `iolog()` использует errno вместо отсутствующего/пустого текста; для явной диагностики код не угадывается |
| `io_bytes` | Исходный `datalen` операции I/O, включая 0; это размер операции, а не итоговый счётчик сессии |
| `payload` | Данные операции I/O только при `log: data`, включая пустую строку для нулевого размера |
| `tcp_info` | Исходный текст TCP_INFO только при `log: tcpinfo` и наличии данных |

Адреса отражают структуры в момент события: доменное имя сохраняется, пока
оно доступно вызывающему коду; после разрешения в адрес завершённое соединение
может содержать IP. При ограничении размера необязательные адресные объекты,
строковые и числовые поля могут опускаться целиком с `truncated: true`.
Общие ключи сохраняются. Исходное текстовое `message` остаётся совместимым
с raw при отсутствии усечения, включая исторический префикс `block` у ошибок.

### 4.3. Закрытие и статистические снимки

`io_delete()` заменяет существующую запись итогов событием `session_close`.
`siginfo()` выдаёт `session_snapshot` с текущими накопленными значениями.
Числа берутся из C-структур, без разбора `message`; существующий raw-текст
и выбор записей правилами сохраняются.

Все перечисленные ниже поля находятся на верхнем уровне JSON-объекта:

| Поле | Смысл |
|---|---|
| `reason` | Только для закрытия: `closed`, `blocked`, `io_error`, `error`, `timeout`, `administrative` |
| `side` | Только для закрытия: сторона исходного дескриптора завершения: `client`, `target`, `control`; `session`, если конкретная сторона неизвестна |
| `timeout` | Только для timeout: `io`, `connect`, `tcp_fin_wait` |
| `scope` | `session`, `control`, `udp_target` или `bind_listener`, подробнее ниже |
| `error.code` | Сохранённый ненулевой errno для `io_error`/`error`; для остальных причин не добавляется |
| `duration_us` | Целая длительность в микросекундах по монотонным часам |
| `idle_us` | Только для снимка: время с последнего I/O в микросекундах |
| `client_bytes_read`, `client_bytes_written` | Прочитано от клиента / записано клиенту |
| `target_bytes_read`, `target_bytes_written` | Прочитано от target / записано target |
| `client_packets_read`, `client_packets_written`, `target_packets_read`, `target_packets_written` | Соответствующие счётчики UDP-пакетов; для TCP отсутствуют |

Известные нули сохраняются; неизвестные времена и адреса опускаются.
Направления client/target и адреса `source`/`destination` учитывают разворот
BIND. Поле `side` фиксируется до переключения активного UDP-сокета.

`scope: session` обозначает TCP-сессию или UDP-снимок без известного target.
`scope: control` обозначает предусмотренный ранее итог client/hostid-rule:
он содержит только клиентские байты управляющего соединения. У TCP этот
сокет также передаёт данные, поэтому его счётчики могут повторять клиентскую
сторону `session`. Дополнительное существующее закрытие BIND имеет
`scope: bind_listener` и длительность, без повторения счётчиков трафика.

`scope: udp_target` относится к существующему сокету/ячейке UDP для семейства
адресов. В Dante это до двух ячеек (IPv4 и IPv6), а не отдельная сессия для
каждого удалённого адреса: счётчики накапливаются внутри ячейки, адрес target
отражает её текущее состояние. Клиентские UDP-байты включают SOCKS-заголовок,
байты target — полезную нагрузку. Длительность закрытия считается от принятия
сессии; возраст UDP-target в снимке — от его первого I/O.

Снимки не являются закрытиями или дельтами трафика: они не содержат `reason`,
не меняют счётчики и не закрывают соединения. При агрегации следует учитывать
`event` и `scope`, чтобы не суммировать повторные снимки и связанные итоги.

### 4.4. Сериализация и ограничения

- Экранировать кавычки, обратные слеши и управляющие символы.
- Сохранять корректный UTF-8; некорректные байты представлять через `\u00XX`.
- Для сигнального пути использовать ограниченный буфер без allocation.
- При нехватке памяти использовать ограниченный fallback, сохраняющий
  корректный JSON и выставляющий `truncated: true`.
- Усекать значения с резервом под завершающие кавычки, скобки и перевод
  строки; не обрезать готовый сериализованный объект произвольным байтом.
- Не добавлять небезопасные для обработчика сигнала зависимости и сохранять
  `errno` по существующему контракту логгера.
- Дамп отладочного кольцевого буфера выдавать одним JSON-событием с
  экранированным содержимым.
- Для pipe/FIFO ограничивать размер сформированной записи размером
  атомарной записи получателя. Учитывать этот предел до вызова записи.
- Проверять конкурентную запись workers и обработку `EINTR`/короткой записи;
  не заявлять атомарность произвольного размера для всех типов получателей.
- Корректность объекта гарантируется на выходе собственного сериализатора;
  внешнее усечение syslog-сервисом находится вне этой гарантии.

### 4.5. Сигналы и специальная диагностика

`signalslog()` формирует `event: message` с помощью двух автоматических
буферов по 16 KiB, без выделения памяти, stdio-форматирования и рекурсивных
сообщений об ошибках. JSON-запись ограничена 16383 байтами вместе с newline;
при усечении входа или сериализации выставляется `truncated: true`.
`errno` сохраняется, включая пустой вектор сообщений и ошибки записи.

В обработчике сигнала для pipe/FIFO используется консервативный предел
`_POSIX_PIPE_BUF`, общий для всех копий записи, без вызова `fpathconf()`.
Вне обработчика сохраняется определение лимита получателя.
Это исключает зависимость от функции, для которой POSIX.1-2008 снял
требование async-signal-safety; см.
[signal-safety(7)](https://man7.org/linux/man-pages/man7/signal-safety.7.html).
Политика syslog остаётся прежней: внутри обработчика вызываются только
критические уровни (`priority <= LOG_CRIT`). Это существующее исключение,
а не гарантия безопасности системной реализации syslog.

При `--enable-livedebug` дамп кольцевого буфера — одна запись `message` с
экранированным содержимым. Порядок и выбор сохраняемой истории не изменены:
подавленные обычные debug-сообщения и записи `signalslog()` попадают в буфер;
обычные выведенные `slog()` туда не добавляются. Дамп не добавляет себя
в историю. При переполнении лимита сохраняется начальная часть дампа,
поэтому последние записи могут быть опущены; `truncated` сообщает об этом.
Повторный снимок буфера не меняет его содержимое.

Обычный `slogstack()` выдаёт JSON-запись для каждого доступного кадра.
В обработчике сигнала он сообщает, что stack trace недоступен: новые вызовы
`backtrace()`/`backtrace_symbols()` туда не добавляются. На платформе без
поддержки backtrace сохраняется отсутствие кадров. `errno` не меняется.

Ошибки самого Flex-сканера в серверном JSON-режиме также проходят через
`signalslog()` с уровнем `error`; исходный exit status 2 сохраняется.
Raw и клиентский сканер используют прежний путь. Сообщения `serr*`,
`swarn*` и синтаксические ошибки парсера уже проходят через общий логгер.

## 5. Поэтапная реализация

Этапы выполняются последовательно. Каждый этап завершается проверками своего
результата. Отметки ниже означают выполнение будущей реализации, а не факт
подготовки этого документа.

### Этап 1. Конфигурация и жизненный цикл настройки

**Зависимости:** зафиксированные решения раздела 3.

**Изменения:**

- [x] Добавить enum `raw/json` и серверное поле формата с нулевым значением,
  соответствующим raw.
- [x] Добавить токен и правило lexer/parser для глобальной директивы.
- [x] Обнаруживать повторную директиву, недопустимое значение и использование
  внутри правила или клиентского конфига.
- [x] Разделить активный формат и состояние «директива встретилась при этом
  разборе», чтобы обеспечить согласованную семантику старта и SIGHUP.
- [x] Не сбрасывать активный формат преждевременно в `resetconfig()`;
  отсутствие директивы обрабатывать после успешного разбора.
- [x] Добавить поле в `compareconfigs()` и проверить наследование/копирование
  конфигурации в новые и существующие дочерние процессы.
- [x] Выводить эффективный формат в диагностике конфигурации.
- [x] Перегенерировать поставляемые `config_parse.c`, `config_parse.h`
  и `config_scan.c` из исходных `.y`/`.l`.

**Проверки:** `-V` для отсутствующей директивы, raw, JSON, неверного и повторного
значения; размещение внутри правила; копирование конфигурации.

**Готовность:** настройка разбирается и распространяется по правилам раздела 3;
клиентская сборка не приобретает зависимости на серверные типы.

**Проверено 2026-09-13 (macOS arm64):**

- Полная сборка сервера, статической и динамической клиентских библиотек.
- 7 тестов `sockd/tests/logformat_config_test.py` с обоими C-драйверами:
  `-V`, значения и области директивы, повторные SIGHUP, клиентский конфиг,
  сохранение формата при сбросе и ошибках, немедленное переключение после
  корректной директивы, fork и копирование конфигурации.
- Существующие тесты statistics API и `ci/smoke.py --relay` прошли.
- Наследование новыми процессами и копирование проверены C-драйвером.
  Путь обновления существующих workers через shared memory проверен по коду;
  runtime-проверка SIGHUP наблюдает эффективный формат mother.
- Проверка на Linux в этой сессии не выполнялась.

Команды запуска тестов и версии генераторов приведены в
[`sockd/tests/README.logformat.md`](../../sockd/tests/README.logformat.md).

### Этап 2. Сериализатор и общий путь журналирования

**Зависимости:** этап 1.

**Изменения:**

- [x] Выделить JSON-сериализатор в небольшой модуль и подключить его к сборке.
- [x] Сохранить сигнатуры `slog()` и `signalslog()`; добавить внутренний
  интерфейс для типизированного события и необязательных счётчиков.
- [x] Реализовать общие поля и экранирование по разделу 4.
- [x] Сохранить маршрутизацию получателей в `dolog()`; для JSON не добавлять
  raw-префикс и не сериализовать уже готовое событие второй раз.
- [x] Сохранить уровни, facility, дублирование в errorlog и debug-фильтрацию.
- [x] Реализовать ограниченное формирование записи, fallback при allocation
  failure и ограничение размера для pipe/FIFO.
- [x] Сохранить raw-ветку и существующий контракт сохранения `errno`.

**Проверки:** разбор результата настоящим JSON-парсером; все общие поля;
кавычки, слеши, управляющие символы, UTF-8, некорректные байты, длинные
сообщения; отказ allocation; получатели и уровни; raw-сравнение с исходной
реализацией при фиксированных времени/PID.

**Готовность:** обычные записи общего логгера имеют корректный JSON во всех
получателях, а raw проходит проверки совместимости.

**Проверено 2026-09-13 (macOS arm64):**

- Полная сборка сервера и обеих клиентских библиотек; `make distdir` включает
  новый модуль, заголовок и logging tests в исходный дистрибутив.
- 10 тестов отдельного сериализатора, включая ASan/UBSan: JSON-парсер Python,
  все байты, UTF-8 и некорректные последовательности, встроенный NUL, границы
  буфера, усечение метаданных, нулевые и максимальные 64-битные счётчики.
- 15 тестов реального логгера: общие поля, роли процессов, типизированное
  событие, уровни/facility, syslog payload, stdout/errorlog/stderr, фильтр debug,
  оба отказа allocation, длинные сообщения, pipe/FIFO, конкурентные писатели,
  EINTR/короткая запись и сохранение `errno`.
- Raw сравнивался побайтно с реализацией `f0e827c` в десяти сценариях при
  фиксированных времени/PID; в тестах сохранены fixtures raw и syslog.
- 8 конфигурационных тестов с C-драйверами, statistics API и smoke с TCP relay
  прошли. Проверка на Linux и с реальным syslog-сервисом не выполнялась.

Сериализатор находится в `lib/logjson.c`, внутренний интерфейс событий и
необязательных счётчиков — в `include/logjson.h`. Лимит каждого буфера —
65536 байт с NUL; fallback — 2048 байт. Для всех копий события используется
минимальный предел участвующих pipe/FIFO. Ограниченные записи сохраняют
обязательные ключи; необязательные числовые поля при нехватке места могут
опускаться целиком с `truncated: true`. Постоянная ошибка I/O может оставить
частичную запись на получателях, допускающих короткую запись.

**Результат этапа 2:** общий путь и типизированный интерфейс подготовлены.
Подключение `iolog()` выполнено на этапе 3 ниже; счётчики сессий относятся
к этапу 4. `signalslog()`, stack trace и кольцевой буфер — к этапу 5.
Команды проверок и ограничения описаны в
[`sockd/tests/README.logformat.md`](../../sockd/tests/README.logformat.md).

### Этап 3. Структурные события `iolog()`

**Зависимости:** этап 2.

**Изменения:**

- [x] Сформировать типизированные события для всех операций из раздела 4.2.
- [x] Передавать правило, verdict, протокол и команду до преобразования
  информации в текст.
- [x] Сериализовать endpoint-структуры, proxy-адреса и доступную информацию
  об аутентификации.
- [x] Сохранить существующие условия `rule->log`, включая connect,
  disconnect, error, data и iooperation.
- [x] Передавать payload и TCP_INFO с существующими ограничениями включения.
- [x] Проверить вызовы из negotiation, request, TCP и UDP, включая неизвестные
  адреса и временные ошибки.

**Проверки:** TCP CONNECT, BIND, UDP ASSOCIATE; IPv4/IPv6/доменное имя;
разрешение и блокировка; ошибка подключения; доступная аутентификация;
пустые/неизвестные значения; включённое и выключенное логирование payload.

**Готовность:** перечисленные события можно фильтровать по структурным полям
без извлечения информации из `message`; число событий соответствует raw.

**Проверено 2026-09-13 (macOS arm64):**

- Полная сборка сервера, статической и динамической клиентских библиотек.
- 9 тестов C-драйвера реального `iolog()`: все 9 операций, 18 событий матрицы
  флагов, правило/verdict, TCP/UDP/BIND, IPv4/IPv6/domain, proxy, аутентификация
  без пароля, неизвестные значения и известные нули, ошибки, бинарный payload,
  TCP_INFO, длинная запись и отказ allocation. Матрица сохраняет текст и число
  событий raw; дополнительно 10 сценариев побайтно сравнены с `be177da`.
- 15 тестов сериализатора, включая ASan/UBSan: вложенные объекты, экранирование,
  границы размеров и опускание необязательных полей с сохранением JSON.
- 10 интеграционных тестов реального сервера без пропусков: CONNECT IPv4/domain
  и IPv6, двусторонний BIND, UDP ASSOCIATE, блокировки, ошибка запроса,
  отказ подключения, data/iooperation/off, TCP_INFO и соответствие raw/JSON.
  Для TCP сравниваются данные потока и суммарные размеры операций, без
  предположения о сохранении границ отдельных отправок при чтении.
- 15 тестов общего логгера, 8 конфигурационных тестов, statistics API и smoke
  с TCP relay прошли. `make distdir` включает новые тесты.
- Native hostid и реальные внешние GSSAPI/PAM/LDAP-сервисы не проверялись;
  proxy и username проверены драйвером. Linux в этой сессии не запускался.

**Результат этапа 3:** все существующие вызовы `iolog()` передают
структурные события; успешный асинхронный CONNECT передаёт TCP_INFO через
исходный указатель. Итоговые счётчики и снимки добавлены далее, на этапе 4.
Команды проверок приведены в
[`sockd/tests/README.logformat.md`](../../sockd/tests/README.logformat.md).

### Этап 4. Закрытие соединений и снимки статистики

**Зависимости:** этап 3.

**Статус:** реализован 2026-09-13; проверен на macOS arm64.

**Изменения:**

- [x] В `io_delete()` передать в событие исходные счётчики, длительность,
  причину завершения и сторону ошибки, сохраняя существующие побочные действия.
- [x] Различать штатное закрытие, блокировку, ошибку I/O, другую ошибку,
  timeout и административное завершение.
- [x] Корректно сопоставить client/target при BIND и развороте сторон.
- [x] Сохранить отдельные события, уже предусмотренные для закрытия BIND.
- [x] В `siginfo()` формировать `session_snapshot` для TCP и UDP, включая
  UDP-сессию без известного target и снимки отдельных UDP-targets.
- [x] Не извлекать числа из строк `Session duration` и `bytes transferred`.

**Проверки:** передать известный объём трафика, сравнить числовые счётчики
с ожидаемыми; проверить длительность, UDP-пакеты, несколько UDP-targets,
BIND и причины закрытия. Убедиться, что snapshot не создаёт фиктивного
события завершения и не меняет данные statistics API.

**Готовность:** итоговую статистику и текущие снимки можно анализировать
по числовым полям; отсутствует двойное логирование из-за новой реализации.

**Реализация:** общий `slogconnection()` дополняет выбранную производителем
запись нативными полями. `io_delete()` формирует `session_close`, сохраняя
условия правил, исходные вызовы учёта statistics API, освобождение ресурсов
и отдельную запись BIND. `siginfo()` формирует снимки из текущих структур
без `io_syncudp()` и без изменения счётчиков. Обработка выполняется в
существующей отложенной части сигнала, вне асинхронного обработчика.
Контракт полей, областей `scope` и агрегации UDP описан в разделе 4.3.

**Проверки:**

- Полная сборка сервера и статической/динамической клиентских библиотек.
- 9 тестов драйвера реальных `io_delete()`/`siginfo()`: все шесть причин,
  три типа timeout, исходный errno, стороны, фильтрация правил, BIND,
  UDP-ячейки и control-счётчики; неизвестные времена опускаются, известные
  нули сохраняются. TCP_INFO передаётся исходным указателем, включая
  длинный текст, без префикса и усечения старого буфера. Снимки не меняют ни сессию, ни массив UDP-targets.
- 10 новых сетевых тестов без пропусков: известный и нулевой TCP-трафик,
  BIND, счётчики байтов и пакетов UDP IPv4/IPv6, снимки без target и
  повторные снимки, idle timeout, reset и отказ подключения, фильтрация
  логов и соответствие raw/JSON. После снимков соединения остаются рабочими.
- 34 raw-сценария побайтно совпали с драйвером этапа 3 при фиксированных
  часах/PID; новые события заменяют существующие записи один к одному.
- 18 тестов сериализатора, также под ASan/UBSan: поля закрытия и снимков,
  64-битные пределы, экранирование и усечение длинных метаданных.
- Регрессии: 15 тестов логгера, 9 тестов `iolog()`, 10 сетевых тестов этапа 3,
  8 конфигурационных тестов, statistics API и smoke с TCP relay прошли.
  `make distdir` включает все новые тестовые файлы.
- Административное завершение проверено драйвером: в Dante нет вызывающего
  его runtime-пути; существующие вызывающие места относятся к Barefoot.
  Linux и внешние сервисы аутентификации в этой сессии не проверялись.

**Результат этапа 4:** закрытия и снимки соединений структурированы.
Сигнальные сообщения и специальные пути диагностики добавлены далее,
на этапе 5. Команды проверок приведены в
[`sockd/tests/README.logformat.md`](../../sockd/tests/README.logformat.md).

### Этап 5. Сигналы, диагностика и полнота покрытия

**Зависимости:** этапы 2–4.

**Статус:** реализован 2026-09-13; проверен на macOS arm64.

**Изменения:**

- [x] Подключить `signalslog()` к формированию корректного JSON с соблюдением
  ограничений сигнального пути.
- [x] Проверить вызовы `serr*`, `swarn*` и диагностику парсера через общий логгер.
- [x] Обеспечить JSON для stack trace и единого дампа кольцевого буфера.
- [x] Проверить сообщения старта, SIGHUP, завершения и stderr fallback
  до и после активации формата.
- [x] Повторно проверить прямые записи в stdout/stderr/syslog и файлы по
  исходникам; отделить записи логгера от CLI, сетевого вывода и тестовых сообщений.
- [x] Проверить, что форматирование не добавляет рекурсивное журналирование
  при ошибке самого сериализатора.

**Проверки:** сигналы, fatal-диагностика в отдельном тестовом процессе,
flush кольцевого буфера, stack trace, debug-фильтрация, длинные многострочные
сообщения и сохранение `errno`.

**Готовность:** после включения JSON особые пути собственного логгера
не добавляют в поток отдельные raw-строки.

**Реализация и аудит:**

- `lib/log.c`: ограниченный JSON-путь `signalslog()`, сохранение errno,
  единый дамп ring buffer и безопасное поведение `slogstack()` в сигнале.
- `lib/config_scan.l` и пересозданный `.c`: серверный `YY_FATAL_ERROR`
  больше не обходит JSON при ошибке чтения или allocation.
- Исправлена доступность объявления `doconfigtest()` для сборки
  `--enable-livedebug`: реализация функции не зависит от `DIAGNOSTIC`,
  а объявление ранее зависело, из-за чего такая сборка не компилировалась.
- Аудит прямых записей: получатели журналов находятся в `lib/log.c`;
  `serr*`/`swarn*` и `parse_util.c` используют общий логгер. Остальные
  прямые записи — CLI help/version/license (`sockd.c`), PID/config state
  files, сетевой/IPC-обмен и statistics HTTP API, клиентские interposition
  hooks, compatibility wrappers или тестовые программы.
- Сгенерированные Bison debug traces остаются разработческой диагностикой:
  `parseconfig()` сбрасывает `yydebug` в 0; включающих присваиваний нет.
  Flex ECHO недостижим при существующих catch-all правилах сканера.

**Проверено:**

- Полная обычная сборка сервера и static/dynamic client, отдельно сервер
  с `--enable-livedebug --disable-client`.
- 12 тестов специальных путей реального логгера, также ASan/UBSan:
  настоящий SIGUSR1, запрет allocation/stdio/time-format/backtrace вызовов
  в сигнальном JSON-пути, errno, пустые сообщения, экранирование, длинные
  записи, pipe/file, syslog exception, debug-фильтрация, ring wrap и
  повторный flush, stack frames, EINTR/short write и отсутствие рекурсии
  при постоянной ошибке записи.
- 22 raw-сравнения с `cfe6d18`: 11 сценариев для pipe и обычного файла;
  stdout, stderr и exit status совпали побайтно.
- 4 теста subprocess-диагностики: `swarn*`, `serr*`, stderr fallback,
  настоящий сбой `fread()` сканера; exit status сохранены.
- 4 интеграционных сценария: старт/SIGUSR1/SIGHUP/завершение, неожиданный
  SIGSEGV, ошибки парсера до/после активации JSON и fatal ring flush в
  live-debug сборке. Последний сценарий запускается с `--livedebug`;
  три остальных проверены также в обычной сборке. Core dumps отключены.
- 79 регрессионных тестов этапов 1–4, statistics API и TCP relay smoke
  прошли. Новые исходники тестов включены в `make distdir`.
- Linux и реальная доставка через syslog-сервис не проверялись; payload и
  маршрутизация syslog проверены драйвером. Ограничения — в разделе 4.5.

**Итог этапа 5:** специальные logging paths завершены. Итоговая матрица,
CI, source archive, live reload, документация и overhead выполнены в этапе 6.

### Этап 6. Интеграционные проверки, документация и CI

**Зависимости:** этапы 1–5.

**Изменения:**

- [x] Добавить модульные проверки формирователя и тестовый запуск по образцу
  существующих проверок в `sockd/tests/`.
- [x] Расширить smoke tests контролируемым SOCKS-трафиком и проверкой каждой
  JSON-строки стандартным JSON-парсером Python.
- [x] Проверить SIGHUP raw → JSON → raw, удаление директивы, обычный reload
  с сохранением JSON и обновление уже работающих workers.
- [x] Проверить одновременную запись нескольких workers в файл и pipe/FIFO,
  а также errorlog и syslog payload.
- [x] Подключить новые проверки к сборкам Linux/macOS и source archive;
  убедиться, что генерируемые parser/scanner и новый модуль входят в архив.
- [x] Выполнить существующие statistics API tests и smoke tests.
- [x] Проверить клиентскую сборку отдельно от server-only package pipeline.
- [x] Дополнить man page и примеры: default raw, включение JSON, схема,
  ранние raw-записи, SIGHUP, внешний syslog-префикс и ограничения усечения.
- [x] Зафиксировать проверенные платформы, результаты тестов и наблюдаемый
  overhead JSON относительно raw на одинаковом наборе событий.

**Базовые существующие проверки** после подготовки сборочного окружения,
из каталога `dante/`:

```sh
make
bash sockd/tests/run-stats-api-tests.sh
python3 ci/smoke.py ./sockd/sockd --relay
```

Новые logging tests должны запускаться из CI рядом с этими проверками.
Они проверяют содержимое и валидность записей, а не только exit code процесса.
Полный запуск теперь доступен командой `bash ci/run-server-tests.sh`.

**Готовность:** завершена матрица проверок раздела 6, инструкции позволяют
включить формат и корректно интерпретировать события, существующие тесты проходят.

**Результат этапа 6:**

- `ci/run-server-tests.sh` объединяет 102 logging-сценария, statistics API
  и smoke с реальным CONNECT, известными байтами, отказом соединения и
  разбором каждой JSON-строки. Обычная сборка выполняет 100 сценариев;
  клиентская проверка и fatal ring runtime проверяются в отдельных вариантах.
- Добавлены live-переходы raw → JSON → JSON → raw → JSON → удаление директивы
  с прежним PID I/O worker и продолжающей работать TCP-сессией. Новые
  соединения проверяются после каждого переключения.
- 40 одновременных UDP-сессий создают минимум два I/O worker; проверяются
  320 I/O-событий на получатель, 6 KB payload, файл/FIFO/stderr и errorlog.
  Дополнительно четыре процесса одновременно пишут длинные warning-записи
  в общий файл и errorlog=file/pipe: по 48 валидных объектов на получатель.
- Linux/macOS package pipeline запускает общий suite из release archive;
  отдельные CI jobs собирают default client и livedebug. Source distribution
  включает generated parser/scanner, сериализатор, тесты, runner и документацию.
- Man page, примеры и пользовательское руководство описывают default/raw,
  схему, early raw, асинхронный reload, severity errorlog, syslog и усечение.
- Полная сверка native producers, конфигурации, всех собственных logging paths
  и сериализатора не выявила незакрытых существенных замечаний. Исправлены
  гонка готовности UDP snapshot-теста и LF-разделение smoke-парсера для
  допустимых U+0085/U+2028/U+2029 внутри JSON-строк.
- Overhead измерен на одинаковых событиях: macOS `/dev/null` 4,29×,
  buffered append-file 1,86× по времени логгера. Это микробенчмарк,
  а не замер пропускной способности прокси; методика приведена в отчёте.
- Локальный `curl` через свежий бинарник подтвердил успешный HTTP-запрос
  и connection refused. В `/tmp/dante-json-stage6` оставлены бинарник, конфиг,
  13 JSON-записей общего журнала и 2 JSON-записи errorlog; все разобраны парсером.

**На чём остановились:** реализация opt-in JSON-логов завершена в границах
разделов 3–4. Следующего этапа реализации нет. Изменения CI подготовлены и
локально проверены; запуск hosted package matrix произойдёт при обычном
запуске workflow. Push и публикация пакетов в эту работу не входили.

## 6. Итоговая матрица приёмки

| Область | Обязательные сценарии | Ожидаемый результат |
|---|---|---|
| Конфигурация | Отсутствие, raw, JSON, неверное/повторное значение, неверное размещение | Default raw; корректные настройки принимаются; остальные диагностируются |
| Raw-совместимость | Фиксированные время/PID, обычные и специальные сообщения | Прежнее оформление и содержание, прежние получатели и фильтры |
| JSON | Unicode, некорректный UTF-8, экранирование, multiline, длинные сообщения | Каждая сформированная строка — валидный объект, без внешнего raw-префикса |
| Ресурсные ограничения | Allocation failure, заполнение буфера, pipe/FIFO | Корректный ограниченный объект, достоверный признак усечения |
| Соединения | CONNECT, BIND, UDP ASSOCIATE, allow/block, ошибки | Поля совпадают с исходными данными события |
| Статистика | Известный трафик, закрытие, timeout, snapshots | Правильные числовые значения и направления; snapshots отличимы от закрытий |
| Специальные пути | Debug, сигналы, stack trace, ring buffer, ошибки парсера | JSON после активации формата; сохранение ограничений сигналов |
| Reload | raw ↔ JSON, удаление директивы, новые и существующие workers | Согласованная семантика переключения с допустимым переходным смешением |
| Получатели | Файл, stdout/stderr, errorlog, syslog payload | Правильное оформление, уровни и маршрутизация |
| Конкурентность | Несколько workers, длинные записи, `EINTR`/короткая запись | Отсутствие повреждения объектов в поддерживаемых условиях записи |
| Регрессии | Linux/macOS server builds, client build, statistics API и smoke tests | Сборки и существующие проверки проходят |

## 7. Завершение и внедрение

Работа завершена, когда после активации `logformat: json` все записи
собственного логгера корректно сериализуются, а соединения, причины их
завершения и итоговая статистика доступны по полям без разбора `message`.

Внедрение остаётся opt-in: обновление бинарного файла само по себе не меняет
формат журналов. Для включения администратор добавляет директиву и выполняет
перезапуск или SIGHUP. Для возврата к прежнему формату указывает `raw` либо
удаляет директиву и перечитывает конфигурацию.

При смене формата существующий лог-файл может содержать записи обоих форматов.
Потребитель должен учитывать границу переключения, а при SIGHUP — также
асинхронное обновление процессов. Ранние сообщения до чтения настройки и
внешнее оформление syslog трактуются согласно разделам 3 и 4.

Основные риски реализации: пропущенные прямые события в `sockd_io.c`,
нарушение ограничений сигнального пути, неверные направления BIND/UDP-счётчиков,
усечение объектов и регрессия raw или клиентской сборки. Для каждого из них
предусмотрены отдельные проверки в этапах и итоговой матрице.
